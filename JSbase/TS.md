### mac 'tsc command not found' in compiling typescript
https://www.codenong.com/39404922/
```
  sudo npm install -g concurrently
  sudo npm install -g lite-server
  sudo npm install -g typescript
```
